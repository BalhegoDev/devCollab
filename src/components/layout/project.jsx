import React from "react";

export default function Project({title}){
    return(
        <section className="project">
            <header>
                <h2>{title}</h2>
            </header>
        </section>
    )
}